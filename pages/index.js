import { Layout, Card } from "../components/index";
import { Flex } from "@chakra-ui/core";
const Home = () => (
  <Layout>
    <Flex p="2rem">
      <Card />
    </Flex>
  </Layout>
);

export default Home;
